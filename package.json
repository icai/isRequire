{
  "name": "@w3cub/isrequire",
  "version": "1.0.4",
  "description": "detect file path is require-able",
  "main": "index.js",
  "scripts": {
    "test": "npm run tstest && mocha",
    "tstest": "mocha -r ts-node/register",
    "release": "standard-version && git push --follow-tags && npm publish --access=public"
  },
  "author": "Terry Cai",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4",
    "mocha": "^6.2.2",
    "standard-version": "latest"
  }
}
